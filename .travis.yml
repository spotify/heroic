language: java

jdk:
  - oraclejdk8

branches:
  except:
    - site

cache:
  directories:
    - $HOME/.m2
    - .cache

# override default command since it doesn't skip checkstyle
install:
  - tools/install-repackaged
